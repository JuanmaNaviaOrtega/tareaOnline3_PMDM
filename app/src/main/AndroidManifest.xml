<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:Android="http://schemas.android.com/apk/distribution">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TareaOnline3"
        tools:targetApi="31">
        <activity
            android:name="MenuActivity"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="tareaOnline3.ejercicio1.MainActivity"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden"/>
        <activity android:name="tareaOnline3.ejercicio1.PreferencesActivity"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden"/>

        <activity
            android:name="tareaOnline3.ejercicio2.MainActivity2"
            android:label="Gestor de Alarmas"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden"/>




        <activity
            android:name="tareaOnline3.ejercicio2.EditarAlarmaActivity"
            android:label="Editar Alarma"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden"/>

        <activity
            android:name="tareaOnline3.ejercicio3.MainActivity3"
            android:exported="true"
            Android:configchanges="orientation|screensize|keyboardhidden"/>
    </application>

</manifest>